# PROGRESS — SIAGA Command Center Platform

Status hidup development. Diperbarui setiap kali sebuah tugas di `TODO.md` berpindah status. Baca ini duluan untuk tahu "sekarang sampai mana" — lihat `CLAUDE.md` untuk konteks arsitektur lengkap.

Terakhir diperbarui: **2026-08-10**

## Ringkasan
Fase 0–2 **selesai**. Fase 3 (Data Layer) **schema selesai, seed diperkaya** — belum diperluas ke skala penuh (50-100 anggota). Fase 4 (Overview & Peta Situasi) **selesai**. Fase 5 (Manajemen Data Anggota) **selesai** — CRUD lengkap dengan RBAC server-side & audit log, diverifikasi dengan Playwright yang menemukan & memperbaiki 1 bug nyata (lihat detail di bawah). Fase 6 (Manajemen Misi & AI Mobilization) **selesai** — OpenAI benar-benar dipanggil (bukan hardcode), dengan fallback deterministik kalau API gagal; diverifikasi end-to-end lewat Playwright termasuk approval & penutupan Misi sungguhan. Fase 7 (Analitik & Laporan) **selesai** — KPI & bar readiness dari data live, 3 laporan + laporan custom yang benar-benar generate file PDF/XLSX asli saat diunduh (bukan file dummy). Fase 8 (AI Chat Assistant) **selesai** — chat grounded ke data Prisma via OpenAI (dua lapis fallback, tidak pernah blank), diverifikasi dengan panggilan OpenAI sungguhan. Fase 9 (Guideline) **selesai** — 3 tab konten (Panduan/FAQ/Modul), disesuaikan jujur ke kondisi implementasi sungguhan (bukan salinan mentah mockup). Fase 10 (Modul Sistem) **selesai** — Pengguna & Role dengan login-block untuk user Nonaktif, Pengaturan dengan 2 toggle yang benar-benar berpengaruh ke sistem, dan Parameter Model AI Mobilization (radius/bobot dari Fase 6) kini benar-benar editable & tersimpan. Fase 11 (Sisi Anggota/Mobile Web) **selesai** — 4 layar mobile penuh dengan approval loop NIK yang benar-benar terhubung ke Command Center, respons notifikasi yang memperbarui kehadiran Misi secara nyata, GPS lokasi sungguhan, dan 1 bug login nyata ditemukan & diperbaiki (redirect ANGGOTA nyangkut di Command Center). Fase 12 (Non-Functional Requirements) **selesai** — audit log immutable di level Prisma Client, NIK terenkripsi AES-256-GCM at-rest (bukan cuma di form), retry/fallback notifikasi, perbaikan aksesibilitas, dan 1 bug regresi nyata (Prisma ikut ter-bundle ke client) ditemukan & diperbaiki di tengah fase ini. Fase 13 (Testing, QA, Deployment Prep) **selesai** — smoke test 4 role × 13 menu Command Center + 4 layar Sisi Anggota via Playwright (nol console error, batas RBAC nyata diverifikasi bukan diasumsikan), 1 rantai Playwright tunggal untuk alur Misi penuh §14.1 (Buat → Generate AI → Setujui → Manajemen Misi & Overview → Tutup → Riwayat Mobilisasi), `app/README.md` untuk onboarding developer, dan target hosting produksi dikonfirmasi tetap TBD (ditanya eksplisit ke user, bukan diasumsikan). **Semua 13 fase rencana awal selesai.** Fase 14 (Penyempurnaan Gap Teknis, di luar rencana awal — atas permintaan & pilihan user) **selesai** — Readiness Score nyata (bukan acak, dihitung ulang otomatis), reminder sertifikasi H-30 nyata, heat-zone layer peta nyata, klik peta membuka drawer CV/Misi lengkap (bukan versi ringkas), geocoding alamat bebas teks (Nominatim) untuk Buat Misi, seed diperluas ke 60 anggota/12 provinsi, polling Overview dipercepat ke 5 detik. Ditemukan & diperbaiki 1 bug nyata (koordinat Pos Komando bentrok pixel dengan kota provinsi, memblokir klik marker). Fase 15 (Rebrand + Tema Visual, atas permintaan & pilihan user) **selesai** — 3 konsep desain dibuat lalu dipilih user (Sentinel HUD), nama produk jadi **SIAGA** (Sistem Identifikasi, Analitik & Gerak Anggota), tema HUD diterapkan global ke 13 menu Command Center + 4 layar Sisi Anggota tanpa mengubah satu pun nilai hex palet FRD §10.2. Ditemukan & diperbaiki 1 bug pra-existing (kontrol zoom Leaflet tertimbun panel Layers sejak Fase 4).

## Status per Fase

| Fase | Status | Catatan |
|---|---|---|
| 0 — Setup Proyek & Infrastruktur | ✅ **Done** | |
| 1 — Design System & App Shell | ✅ **Done** | Komponen UI reusable + Drawer/Modal primitive selesai |
| 2 — Auth & RBAC | ✅ **Done** | Login, session, RBAC redirect, sign-out — semua diverifikasi via curl end-to-end (lihat detail di bawah) |
| 3 — Data Layer (Prisma Schema + Seed) | 🟡 **In Progress** | Schema lengkap (`kodeAnggota` ditambahkan Fase 4). Seed diperkaya di Fase 5: tanggal lahir, pekerjaan sipil, kontak darurat, 2 sertifikasi/anggota, riwayat Pelatihan personal. Masih 20 anggota (belum 50-100); `calculateReadinessScore()` belum ada (nilai seed masih acak, bukan hasil formula) |
| 4 — Modul Overview & Peta Situasi | ✅ **Done** | Peta Leaflet nyata (bukan mock), layers, training panel, 3 panel bawah dengan agregasi data asli, show/hide individual & sekaligus. Diverifikasi visual + console-error check pakai Playwright headless |
| 5 — Modul Manajemen Data Anggota | ✅ **Done** | Direktori + Drawer CV lengkap + Kompetensi&Sertifikasi + Riwayat Pelatihan + CRUD (create/edit/nonaktifkan/ubah status siaga) dengan RBAC di server action + audit log. Diverifikasi Playwright — nemu 1 bug nyata (stale action binding), sudah diperbaiki & diverifikasi ulang |
| 6 — Modul Manajemen Misi & AI Mobilization | ✅ **Done** | Buat Misi (modal global dari topbar) → OpenAI grounded scoring+ringkasan+ETA → approval+notifikasi → Manajemen Misi (tabel+drawer+kehadiran) → Tutup Misi+evaluasi. Fallback deterministik kalau OpenAI gagal. Diverifikasi Playwright dengan OpenAI asli terpanggil |
| 7 — Modul Analitik & Laporan | ✅ **Done** | KPI + bar Readiness per wilayah dari data live; Laporan & Ekspor generate PDF/XLSX ASLI (pdfkit/exceljs) saat diunduh, bukan file dummy; Riwayat Mobilisasi dari Misi Selesai. Diverifikasi Playwright termasuk unduh file sungguhan |
| 8 — Modul AI Chat Assistant | ✅ **Done** | Chat grounded ke `ChatContext` (agregasi Prisma nyata) via OpenAI structured output; fallback keyword-matching kalau API gagal; percakapan awal pre-filled dari data nyata. Diverifikasi Playwright dengan OpenAI asli terpanggil |
| 9 — Modul Guideline | ✅ **Done** | 3 tab (Panduan/FAQ/Modul), konten disembunyikan via CSS bukan dihapus dari DOM. Istilah & status modul disamakan jujur ke UI/progres sungguhan, bukan salinan mentah mockup |
| 10 — Modul Sistem | ✅ **Done** | Pengguna & Role (CRUD + login-block user Nonaktif + self-protection), Pengaturan (2 toggle nyata + Parameter Model AI Mobilization editable, closing gap Fase 6). Diverifikasi Playwright termasuk login akun nonaktif ditolak sungguhan |
| 11 — Sisi Anggota (Mobile Web) | ✅ **Done** | 4 layar (Beranda/Profil/Riwayat/Notifikasi), approval NIK terhubung ke drawer CV Command Center, respons notifikasi mengubah kehadiran Misi sungguhan, GPS lokasi nyata. Ditemukan & diperbaiki 1 bug login nyata di jalur ini. Diverifikasi Playwright lintas dua sisi (Anggota + Operator) |
| 12 — Non-Functional Requirements | ✅ **Done** | Audit log immutable (Prisma `$extends` guard), NIK terenkripsi AES-256-GCM at-rest + `nikHash` untuk lookup, retry/fallback notifikasi, target klik 30px + kontras `--ink-3` diperbaiki. Ditemukan & diperbaiki 1 bug regresi nyata (Prisma bocor ke client bundle). Diverifikasi Playwright + query SQLite mentah |
| 13 — Testing, QA, Deployment Prep | ✅ **Done** | Smoke test 4 role × semua route (RBAC nyata, bukan asumsi), 1 rantai Playwright alur Misi penuh §14.1, `app/README.md`, catatan deployment (hosting tetap TBD atas konfirmasi user) |
| 14 — Penyempurnaan Gap Teknis (di luar rencana awal) | ✅ **Done** | Readiness Score nyata, reminder sertifikasi H-30 nyata, heat-zone layer nyata, drawer peta jadi CV/Misi lengkap, geocoding Nominatim, seed 60 anggota/12 provinsi, polling 5 detik. 1 bug nyata (Pos Komando bentrok koordinat) ditemukan & diperbaiki |
| 15 — Rebrand SIAGA + Tema Sentinel HUD (di luar rencana awal) | ✅ **Done** | 3 konsep desain dibuat & dipilih user, nama produk jadi SIAGA, tema HUD diterapkan global ke 13 menu + 4 layar mobile tanpa mengubah satu pun hex FRD §10.2. 1 bug pra-existing (kontrol zoom Leaflet tertimbun panel Layers) ditemukan & diperbaiki |

## Detail — Apa yang Sudah Jadi

**Fase 0 & 1** — lihat riwayat sebelumnya (Node.js portable, Next.js 16 + Tailwind v4 scaffold, Prisma schema, shell navigasi). Tambahan sesi ini:
- Komponen UI reusable di `src/components/ui/`: `Button` (variant default/outline/solid/danger/ghost), `Badge` (+ helper `statusSiagaColor`/`statusMisiColor`/`urgensiColor`/`statusSertifikasiColor` — badge status FRD §10.8 selalu ada label teks, bukan cuma warna), `Card`/`CardHeader`/`CardTitle`/`CardContent`, `Input`/`Textarea`/`Select`/`Label`, `Chip`
- `Drawer` (slide dari kanan, untuk profil CV anggota / detail Misi nanti) & `Modal` (untuk form Buat Misi nanti) — keduanya Radix Dialog based, animasi didefinisikan di `globals.css` (`overlay-show`, `modal-show`, `drawer-slide-in`)
- **Next.js 16 migration**: `middleware.ts` di-rename ke `src/proxy.ts` (Next 16 mendeprecate nama "middleware", lihat `node_modules/next/dist/docs/.../proxy.md`) — build sekarang bersih tanpa warning deprecation

**Fase 2 — Auth & RBAC (selesai, diverifikasi end-to-end):**
- `src/lib/auth.ts` — Auth.js v5, Credentials provider, password di-hash bcrypt, session JWT dengan `role` & `anggotaId` di token (lihat `src/types/next-auth.d.ts` untuk type augmentation — **catatan penting**: augmentasi `JWT` harus target modul `@auth/core/jwt`, BUKAN `next-auth/jwt` yang cuma re-export dan tidak ke-merge oleh TypeScript)
- `src/app/login/` — halaman login (Server Action `loginAction` via `useActionState`, bukan client-side `signIn` dari `next-auth/react`) + `src/lib/auth-actions.ts` (`signOutAction`)
- `src/proxy.ts` — RBAC: belum login → redirect `/login`; role `ANGGOTA` terkunci ke `/m/*`; role lain (Super Admin/Operator/Analis) terkunci ke Command Center; sudah login tapi buka `/login` → redirect otomatis ke area masing-masing
- Topbar & Sidebar sekarang menerima `user` (session asli) dari `(command)/layout.tsx` (server component, panggil `auth()`) — nama, role label, dan tombol Keluar semua nyata, bukan dekoratif lagi
- **Diverifikasi manual via curl** (bukan cuma asumsi): login benar → cookie session ter-set & `/overview` menampilkan nama+role asli; password salah → redirect `?error=CredentialsSignin`; role ANGGOTA ke `/overview` → auto-redirect ke `/m`; role lain ke `/m` → auto-redirect ke `/overview`; sign-out → cookie ter-clear, `/overview` kembali redirect ke `/login`

**Fase 3 (sebagian) — Seed data:**
- `prisma/seed.ts`: 4 akun demo (satu per role, password sama untuk semua: `komcad123`) + 20 anggota dummy tersebar 8 provinsi dengan profil demografi, lokasi, sertifikasi; 2 aktivitas pelatihan; 2 Misi contoh (satu Selesai dengan evaluasi, satu Dimobilisasi dengan Penugasan berstatus "Menunggu Respons")
- Akun `anggota@komcad.mil.id` ditautkan ke salah satu record Anggota (bukan user tanpa data) supaya Sisi Anggota nanti punya sesuatu untuk ditampilkan
- Idempotent untuk user (pakai `upsert`), tapi anggota/misi/aktivitas pakai `create` biasa — **menjalankan `npm run db:seed` dua kali akan duplikasi anggota/misi**. Kalau perlu reset: hapus `app/prisma/dev.db` lalu `npm run db:push && npm run db:seed`

**Fase 4 — Overview & Peta Situasi (selesai, diverifikasi dengan browser sungguhan):**
- `src/lib/overview-data.ts` — semua data panel Overview di-query & diagregasi dari Prisma (bukan hardcode): posisi anggota siap/siaga dari `Lokasi` terbaru, zona Misi aktif, statistik gender/provinsi via `groupBy`, feed Misi terbaru, ringkasan AI Mobilization dari `Misi` berstatus Dimobilisasi + `Penugasan`-nya
- `src/lib/pos-komando.ts` — daftar lokasi Pos Komando sebagai konstanta statis (bukan entitas Prisma, karena FRD tidak mendefinisikan atribut/CRUD untuk data ini — didokumentasikan sebagai asumsi FRD §11)
- `src/components/overview/situation-map.tsx` — react-leaflet, dynamic-imported dengan `ssr:false` (Leaflet butuh `window`), dibatasi `maxBounds` Indonesia, filter CSS tactical dark (`.tactical-tiles` di `globals.css`), auto `invalidateSize` via `ResizeObserver` (jadi otomatis benar baik saat mode layar penuh maupun saat sidebar di-collapse, tidak perlu wiring manual ke event toggle manapun)
- Panel Layers, Legend, Training Panel (klik → Drawer), 3 panel bawah (Statistik/Misi Terbaru/AI Mobilization) dengan show/hide individual + show/hide semua sekaligus — state di-manage di satu client component orchestrator `overview-view.tsx`
- Klik marker anggota/zona Misi membuka Drawer ringkas (bukan profil CV lengkap — itu Fase 5) — reuse `Drawer` primitive dari Fase 1
- **Keamanan**: awalnya feed Misi Terbaru dirakit sebagai string HTML lalu di-render dengan `dangerouslySetInnerHTML` (niru pola mockup). Diperbaiki sebelum commit — field `Lokasi` pada Misi adalah teks bebas yang diisi Operator (FR-08), jadi berpotensi stored-XSS kalau di-render sebagai HTML mentah. Sekarang dirender sebagai JSX biasa (auto-escaped), tidak ada `dangerouslySetInnerHTML` di modul ini
- **Perbaikan skema**: `Anggota` awalnya cuma punya primary key cuid internal, sehingga popup peta menampilkan ID acak (`cmsn0g1oa...`) alih-alih ID yang manusiawi. Ditambahkan field `kodeAnggota` (format `ANG-00001`, unique) — relevan juga untuk Fase 5 (tabel Direktori Anggota di mockup menampilkan kolom ID seperti ini)
- **Verifikasi**: dites pakai Playwright headless Chromium (bukan sekadar curl, karena peta Leaflet cuma jalan di client) — login berhasil, 15 tile peta termuat, 18 shape interaktif (marker/circle) muncul, klik marker membuka Drawer dengan data benar, klik "−" pada panel menyembunyikannya dan memunculkan chip "+ Nama Panel", nol console error. Playwright ditambahkan sebagai devDependency untuk verifikasi browser di sesi-sesi berikutnya

**Fase 5 — Manajemen Data Anggota (selesai, RBAC server-side + CRUD teruji end-to-end):**
- `src/lib/anggota-data.ts` — `getAnggotaFullList()` memuat seluruh detail (profil, lokasi, sertifikasi, pelatihan, penugasan) sekaligus untuk 20 anggota (didokumentasikan eksplisit di kode: kalau data mendekati skala NFR-02/500rb, ini WAJIB diganti pencarian & drawer server-side per-baris, bukan kirim semua ke client)
- `src/lib/sertifikasi.ts` — `computeSertifikasiStatus()` menghitung status (Aktif/Akan Kedaluwarsa/Kedaluwarsa) dari `tanggalBerlaku` setiap kali di-query, BUKAN dipercaya dari kolom `status` tersimpan — ini yang bikin FR-06 ("badge berubah warna otomatis") benar-benar valid, bukan cuma snapshot statis. Dipakai konsisten di Direktori Anggota, drawer CV, dan menu Kompetensi & Sertifikasi
- `src/lib/anggota-actions.ts` — Server Actions `createAnggotaAction`/`updateAnggotaAction`/`deactivateAnggotaAction`/`updateStatusSiagaAction`, semua: (1) cek role via `auth()` di dalam action itu sendiri (RBAC di server, bukan cuma sembunyi tombol di client — Analis yang coba manggil action tetap ditolak), (2) validasi Zod (NIK regex 16 digit), (3) tulis `AuditLog` via helper baru `src/lib/audit-log.ts`, (4) `revalidatePath` supaya UI ter-update otomatis tanpa reload manual
- Drawer CV: foto placeholder siluet, data pribadi (usia dihitung dari tanggal lahir), kompetensi (diturunkan dari daftar jenis Sertifikasi — skema tidak punya field kompetensi terpisah dari sertifikasi formal, disederhanakan secara sadar), sertifikasi, riwayat pelatihan, riwayat penugasan (join ke Misi), tombol ubah status siaga + edit + nonaktifkan (khusus Super Admin/Operator)
- **Bug nyata ditemukan & diperbaiki lewat testing Playwright** (bukan cuma "kelihatan jalan"): alur Edit → Escape → Tambah Anggota → Simpan ternyata memanggil `updateAnggotaAction` dengan id anggota LAMA, bukan `createAnggotaAction` — root cause: `AnggotaFormModal` dipakai berulang untuk create & edit dengan `action` yang berbeda tiap kali (`updateAnggotaAction.bind(null, id)` vs `createAnggotaAction`), dan komponennya tidak pernah unmount sehingga `useActionState` di dalamnya bisa membawa binding lama. Diperbaiki dengan `key={formTarget ? `edit-${id}` : "create"}` supaya React memaksa remount komponen (dan hook di dalamnya) tiap kali target berpindah. Reproduksi & fix dikonfirmasi lewat pengecekan log server (`createAnggotaAction` vs `updateAnggotaAction` yang benar-benar terpanggil), bukan cuma asumsi dari UI yang terlihat benar
- **Verifikasi lengkap**: search/filter tabel, buka drawer, ubah status siaga (server action beneran ke-invoke, dicek dari log), buka modal edit dengan data ter-isi benar, create anggota baru (row baru muncul otomatis lewat `revalidatePath`, tanpa reload), halaman Sertifikasi & Pelatihan — semua lewat browser Playwright sungguhan, nol console error di sepanjang alur

**Fase 6 — Manajemen Misi & AI Mobilization (selesai, AI benar-benar dipanggil & diverifikasi):**
- `src/lib/geo.ts` — Haversine + estimasi ETA (asumsi kecepatan gabungan 40 km/jam + 10 menit persiapan, didokumentasikan sebagai asumsi FRD §11, bukan hasil routing API sungguhan)
- `src/lib/wilayah.ts` — 8 kota referensi (sama dengan sebaran provinsi seed) dipakai sebagai dropdown "Lokasi" di form Buat Misi, supaya tiap Misi punya koordinat pasti untuk kalkulasi jarak/ETA. **Simplifikasi disengaja**: bukan input teks bebas + geocoding sungguhan seperti mockup — produksi butuh geocoding API
- `src/lib/misi-data.ts` — `getKandidatPool()` (anggota Aktif/Siaga terdekat dari lokasi Misi via Haversine, diambil pool 15 teratas untuk diserahkan ke AI), `getMisiListFull()`/`getMisiKpi()`/`getMisiMenungguApproval()` untuk halaman Manajemen Misi & AI Mobilization
- `src/lib/ai-mobilization.ts` — `generateAiMobilizationRecommendation()`: panggil OpenAI (`gpt-4o-mini`) dengan `response_format: json_schema` (`anggotaId` di tiap kandidat di-*enum*-kan ke daftar pool asli — AI **tidak bisa** mengarang personel di luar itu, hanya bisa memilih & menjelaskan dari yang diberi). Kalau `OPENAI_API_KEY` kosong atau panggilan API gagal (network/timeout/parsing), otomatis jatuh ke **fallback deterministik** (formula readiness×0.4 + jarak×0.3 + kompetensi×0.2 + jeda×0.1) — ringkasan AI di UI menandai eksplisit "mode fallback" kalau ini terjadi, supaya Operator tahu itu bukan hasil AI sungguhan
- `src/lib/misi-actions.ts` — `generateMisiAction` (buat Misi status Draft + Penugasan dari hasil AI, audit log `AI_MOBILIZATION_GENERATE`), `approveMisiAction` (Draft→Dimobilisasi, kirim `Notifikasi` ke tiap kandidat, audit log `APPROVE_MISI`), `closeMisiAction` (Dimobilisasi→Selesai + evaluasi, semua Penugasan jadi "Selesai", audit log `CLOSE_MISI`), `updateKehadiranAction` (dropdown manual per personel, audit log `UPDATE_KEHADIRAN`) — RBAC ditegakkan di tiap action (Super Admin/Operator saja), sama seperti pola Fase 5
- `BuatMisiModal` — dipasang sekali secara global di `AppShell` (bukan per-halaman), state wizard 4 langkah (form → loading → result → done) dikelola lokal, jadi tombol BUAT MISI di topbar jalan dari halaman manapun seperti di mockup. Tombol topbar sendiri sekarang digate role (Analis tidak melihatnya sama sekali, bukan cuma disembunyikan CSS)
- Menu Manajemen Misi (`/misi`): KPI row (Misi Aktif, Selesai bulan ini, rata-rata waktu berkumpul, personel termobilisasi — semua dihitung dari data asli), search+filter chip, drawer detail dengan kehadiran & tutup-misi
- Menu AI Mobilization (`/ai-mobilization`): daftar Misi Draft menunggu approval (bisa langsung disetujui dari sini juga) + kartu Parameter Model (radius/bobot) — **catatan jujur**: parameter ini masih nilai statis di kode, belum ada UI Admin untuk mengubahnya (persistensi pengaturan ditunda ke Fase 10, supaya tidak scope-creep di sini)
- **Verifikasi Playwright end-to-end dengan API sungguhan** (bukan mock OpenAI): login Operator → buka Misi Dimobilisasi yang sudah ada dari seed → ubah kehadiran salah satu personel (dropdown) → berhasil tersimpan → klik BUAT MISI di topbar → isi form → submit → **`sumber: "openai"` terkonfirmasi** (bukan fallback) → ringkasan AI menyebut nama Pemberi Perintah persis & mengutip Deskripsi Misi → 3 kandidat dengan skor/alasan (≥3 poin, mengutip angka jarak/readiness/kompetensi nyata)/ETA → Setujui & Kirim Notifikasi → kode Misi baru (`MISI-2026-XXX`) muncul di tabel → buka drawer Misi itu lagi → Tutup Misi & Evaluasi → status berubah jadi Selesai. **Nol console error** di semua langkah. Data uji dibersihkan, dev database di-reset & di-reseed ulang ke starter set bersih sebelum commit (lihat catatan izin reset di bawah)

**Fase 7 — Analitik & Laporan (selesai, file laporan digenerate sungguhan):**
- `src/lib/analitik-data.ts` — `getAnalitikKpi()` (readiness nasional dari `aggregate`, Misi Selesai 30 hari, sertifikasi kedaluwarsa dihitung ulang via `computeSertifikasiStatus` bukan kolom stale, **AI Mobilization Uptime dari data nyata**: `AuditLog` aksi `AI_MOBILIZATION_GENERATE` di-parse metadata-nya untuk hitung persentase `sumber: "openai"` vs fallback — pengganti kartu "UPTIME SISTEM" di mockup yang di app ini tidak ada infrastruktur monitoring sungguhan untuk diukur), `getReadinessPerWilayah()` (group by provinsi, urut skor tertinggi)
- `src/lib/laporan.ts` — generator laporan **sungguhan** dari Prisma, bukan file placeholder: `generateLaporanKesiapsiagaanPdf`/`generateEvaluasiAiPdf` (`pdfkit`), `generateRekapMobilisasiXlsx`/`generateLaporanWilayahXlsx`/`generateLaporanMisiXlsx`/`generateLaporanPeriodeXlsx` (`exceljs`) — semua dipanggil on-demand lewat Route Handler (`src/app/api/laporan/{kesiapsiagaan,rekap-mobilisasi,evaluasi-ai,custom}/route.ts`), bukan cron/file tersimpan, jadi datanya selalu terkini saat diunduh
- RBAC: unduh laporan dibatasi Super Admin & Analis (`ROLES.SUPER_ADMIN`/`ROLES.ANALIS`) sesuai kolom Aktor FR-27 di FRD — halaman `/laporan` tetap bisa dibuka role Command Center lain tapi tombol Unduh diganti non-aktif dengan pesan penjelasan, bukan disembunyikan diam-diam
- **Bug nyata ditemukan & diperbaiki**: `pdfkit` gagal di runtime dengan `ENOENT ...Helvetica.afm` — root cause: Turbopack membungkus Route Handler sehingga `__dirname` pdfkit jadi path virtual (`C:\ROOT\...`), bukan lokasi asli paket di `node_modules`, jadi file metrik font bawaannya tidak ketemu saat pdfkit coba baca via `fs` relatif ke `__dirname`. Diperbaiki dengan `serverExternalPackages: ["pdfkit"]` di `next.config.ts` (opsi resmi Next.js untuk paket yang punya asset non-JS dibaca relatif ke lokasi filenya sendiri — jangan di-bundle, require() langsung dari `node_modules`). Ditemukan lewat pengecekan `dev-server.log`, bukan cuma dari respons 500 di Playwright
- **Verifikasi Playwright**: login Analis → Analitik (KPI + 8 bar wilayah tampil, terurut skor tertinggi) → Riwayat (tabel Misi Selesai) → Laporan & Ekspor → unduh ketiga laporan tetap (ukuran file wajar, bukan 0 byte) → isi & submit form custom Per Wilayah → file ke-generate. PDF hasil unduh dibaca ulang manual (bukan cuma cek ukuran) untuk pastikan teksnya benar, rapi, dan tidak corrupt. Nol console error

**Fase 8 — AI Chat Assistant (selesai, grounded ke data nyata, dua lapis fallback):**
- `src/lib/ai-chat-data.ts` — `getChatContext()`: satu snapshot agregasi Prisma (total anggota, breakdown status siaga/gender/pendidikan, Readiness nasional & per wilayah via `getReadinessPerWilayah` dari Fase 7, Misi aktif, sertifikasi kedaluwarsa, jumlah anggota belum pelatihan 3 bulan) — inilah SATU-SATUNYA sumber kebenaran yang boleh dipakai model untuk menjawab, dikirim utuh sebagai JSON ke OpenAI tiap request
- `src/lib/ai-chat.ts` — `askAiChat()`: OpenAI (`gpt-4o-mini`, `response_format: json_schema`) diberi instruksi tegas "JAWAB HANYA dari data JSON ini", mengembalikan `{cocok, jawaban, tabel}` — kalau `cocok:false` (pertanyaan di luar topik), sistem override dengan pesan fallback tetap (tidak percaya penuh ke teks bebas model untuk kasus ini, supaya konsisten). `fallbackAnswer()` — keyword-matching lokal (mirror pola `chatKB` di mockup tapi angkanya dari `ChatContext` asli, bukan dikarang) — dipakai kalau `OPENAI_API_KEY` kosong atau API call gagal, DAN dipakai ulang di `page.tsx` untuk membangun 2 contoh percakapan awal tanpa perlu memanggil OpenAI di setiap page load
- `src/lib/ai-chat-actions.ts` — `askChatAction()` (Server Action dipanggil langsung dari client, pola sama dengan Fase 6): cek sesi, validasi panjang pertanyaan, panggil `getChatContext()` fresh tiap kali (data selalu terkini) lalu `askAiChat()`
- `ChatView` — bubble kiri (AI, ikon Sparkles)/kanan (user, ikon User), tabel jawaban dirender JSX biasa dari `{headers, rows}` (bukan `dangerouslySetInnerHTML` — pelajaran dari bug Fase 4 diterapkan lagi di sini), indikator mengetik (bouncing dot) tampil selama `useTransition` pending sungguhan, chip pertanyaan cepat klik-langsung-kirim
- **Verifikasi Playwright dengan OpenAI sungguhan**: login → intro pre-filled tampil dengan angka nyata (bukan "48.210 anggota" fiktif dari mockup) → klik chip Misi aktif → indikator mengetik muncul → jawaban benar (dikonfirmasi durasi request ~1.4 detik di log server, bukan instan sehingga bukan cache/mock) → pertanyaan di luar topik ("Siapa presiden Indonesia?") → fallback graceful, bukan blank. Nol console error

**Fase 9 — Guideline (selesai, konten disesuaikan jujur ke implementasi sungguhan):**
- `src/components/guideline/guideline-view.tsx` — satu client component, state `tab` lokal, 3 seksi (Panduan/FAQ/Modul) dirender selalu tapi divisibilitas lewat class `hidden` (bukan conditional render `{tab === x && ...}`) — memenuhi FR-36 "disembunyikan bukan dihapus dari DOM" secara harfiah, diverifikasi lewat `count()` Playwright bukan cuma `isVisible()`
- **Konten sengaja tidak disalin mentah dari mockup**, disesuaikan ke kondisi sungguhan platform ini:
  - Panduan: istilah tombol/menu disamakan persis ke `COMMAND_NAV` & label UI aktual ("BUAT MISI" bukan "Buat Misi Baru", dst)
  - FAQ "kandidat menolak notifikasi": mockup mengklaim auto-reassign otomatis ke kandidat berikutnya — fitur itu TIDAK ada di `misi-actions.ts` Fase 6, jadi jawaban ditulis ulang jujur (kandidat cadangan tetap terlihat di drawer, dikoordinasikan manual)
  - FAQ "siapa bisa ubah profil anggota": ditambahkan catatan eksplisit bahwa Sisi Anggota (self-service) masih dalam pengembangan (Fase 11 belum dikerjakan), bukan mengklaim fitur itu sudah tersedia
  - FAQ baru soal kanal notifikasi SMS/WhatsApp: jujur menyebut ini simulasi in-app (`channel: "Aplikasi"`), bukan integrasi produksi — transparansi ke gap NFR-07
  - Badge status modul (Aktif/Beta) di tab Modul mengikuti progres pengembangan sungguhan per fase ini: Modul 1/2/3/5 "Aktif" (sudah dibangun & diverifikasi Fase 5–8), Modul 4 "Beta" (Sisi Anggota memang masih placeholder)
- **Verifikasi Playwright**: tab default Panduan tampil, klik FAQ → konten Panduan tetap `count()>0` di DOM (cuma disembunyikan CSS, bukan di-unmount React), klik Modul → 5 kartu tampil dengan badge benar. Nol console error

**Fase 10 — Modul Sistem (selesai, 2 toggle preferensi nyata + gap Fase 6 ditutup):**
- Schema: `User.status` (Aktif/Nonaktif, default Aktif) ditambahkan lewat `prisma db push` non-destruktif (bukan `--force-reset`, kolom baru dengan default diterapkan ke baris lama tanpa kehilangan data); model baru `PengaturanSistem` — singleton (`id: "default"`, dibuat otomatis via `upsert` di `getPengaturanSistem()`, tidak perlu ubah seed script)
- `src/lib/user-data.ts`/`user-actions.ts` — CRUD Pengguna Command Center (Super Admin/Operator/Analis; akun Anggota TIDAK muncul di sini, tetap dikelola lewat modul Data Anggota). Guard self-protection: tidak bisa menonaktifkan akun sendiri atau menurunkan role sendiri dari Super Admin — cegah admin mengunci diri sendiri keluar sistem. `auth.ts` menolak login kalau `user.status === "Nonaktif"` (pesan generik sama seperti password salah — tidak membocorkan status akun ke calon penyerang)
- `src/lib/pengaturan-data.ts`/`pengaturan-actions.ts` — preferensi Notifikasi & Peta plus Parameter Model AI Mobilization, SUPER_ADMIN only, semua lewat audit log
- **Toggle preferensi dibagi jujur jadi 2 kategori** — bukan semua "berfungsi" cuma karena ada di UI:
  - **Benar-benar berpengaruh**: "Notifikasi Misi baru" (dibaca `approveMisiAction` sebelum membuat record `Notifikasi` — dites nyata: dimatikan → approve Misi → pesan konfirmasi bilang 0 notifikasi terkirim, BUKAN mengklaim kirim padahal tidak) dan "Auto-refresh data peta" (mengaktifkan `setInterval(() => router.refresh(), 15000)` di `OverviewView` — polling, bukan push real-time sungguhan, tapi menutup sebagian gap FR-07/FR-24 yang sebelumnya cuma refresh-on-navigate)
  - **Belum berpengaruh, dan UI-nya bilang begitu secara eksplisit**: reminder sertifikasi H-30 terpisah, fallback SMS, heat-zone peta — mesin/layer-nya memang belum ada, jadi deskripsi di UI langsung bilang "belum berpengaruh ke sistem" alih-alih pura-pura berfungsi
- **Parameter Model AI Mobilization sekarang genuinely editable** (menutup gap yang didokumentasikan sejak Fase 6/PROGRESS sebelumnya): radius + 3 bobot (harus total 100%, divalidasi Zod `.refine()` + tombol Simpan disabled di client kalau belum 100%) disimpan di `PengaturanSistem`, dibaca `generateMisiAction` tiap Misi baru — `getKandidatPool()` sekarang menerima `radiusKm` sebagai filter jarak keras (dengan fallback ke kandidat terdekat kalau radius terlalu ketat sampai hasil kosong, supaya demo dengan seed data yang tersebar antar-provinsi tidak buntu), dan bobot dipakai baik di formula `skorFallback()` (matematis, direkonstruksi supaya total tetap 100% termasuk porsi "jeda penugasan" yang sengaja tidak dijadikan parameter Admin terpisah) maupun dikirim sebagai instruksi ke prompt OpenAI (panduan, bukan aturan matematis ketat untuk jalur AI)
- **Verifikasi Playwright cukup menyeluruh** (mencakup skenario yang gagal di percobaan pertama — dicatat sebagai pembelajaran, bukan disembunyikan): tambah pengguna → edit role → nonaktifkan → **sign-out lalu login pakai akun ter-nonaktifkan itu sungguhan gagal** (bukan diasumsikan dari baca kode) → tombol nonaktifkan-diri-sendiri disabled → matikan toggle notifikasi, Buat Misi, Setujui, pesan "0 notifikasi terkirim" muncul benar → ubah radius jadi 999km, tersimpan & langsung tampil di halaman AI Mobilization → validasi total bobot ≠ 100% menonaktifkan tombol Simpan. Nol console error di semua langkah
- **Catatan proses**: dua kali percobaan pertama skrip Playwright gagal karena skrip TIDAK idempotent (bukan bug aplikasi) — pertama karena lupa sign-out sebelum tes login (proxy auto-redirect `/login` kalau sudah ada sesi aktif, sesuai perilaku yang memang didesain di Fase 2), kedua karena toggle "Notifikasi Misi baru" diklik tanpa cek status awal padahal ada sisa state dari percobaan gagal sebelumnya (bug test klasik: asumsi state awal padahal ada leftover). Kedua kasus dikonfirmasi lewat pengecekan `AuditLog`/query DB langsung sebelum disimpulkan, bukan ditebak

**Fase 11 — Sisi Anggota / Mobile Web (selesai, approval loop & sinkronisasi lintas modul beneran nyambung):**
- `src/lib/anggota-mobile-data.ts` — `requireSelfAnggotaId()`: satu pintu wajib untuk SEMUA data-fetcher/Server Action Sisi Anggota, selalu ambil `anggotaId` dari sesi login (`session.user.anggotaId`), TIDAK PERNAH dari parameter yang bisa dikontrol client — inilah data isolation FR-38 ditegakkan secara struktural, bukan cuma query filter yang bisa lupa ditambahkan
- Schema baru: `PermintaanUbahData` (singleton per pengajuan, status Menunggu/Disetujui/Ditolak) untuk approval NIK (FR-37), plus relasi `Notifikasi.misi` yang ternyata belum ada sebelumnya (field `misiId` cuma string biasa, bukan foreign key relation asli — ditambahkan sekarang supaya bisa `include` data Misi di kartu notifikasi)
- `src/lib/anggota-mobile-actions.ts` — `updateProfilSelfAction` (field bebas langsung tersimpan; NIK masuk `PermintaanUbahData` kalau berubah, TIDAK langsung menulis ke `Anggota.nik`), `updateStatusSiagaSelfAction` (self-only, terpisah dari versi Admin/Operator supaya Anggota tidak bisa mengubah status anggota lain), `updateLokasiSelfAction` (menerima koordinat dari Geolocation API browser, membuat record `Lokasi` baru yang langsung dipakai marker Overview), `respondNotifikasiAction` (verifikasi kepemilikan notifikasi sebelum proses — anggota tidak bisa merespons notifikasi orang lain meski tahu ID-nya — DAN memperbarui `Penugasan.statusKehadiran` supaya Operator langsung lihat perubahan di Manajemen Misi, bukan cuma tercatat sepihak di sisi Anggota)
- `src/lib/permintaan-actions.ts` — sisi Command Center: `approvePermintaanNikAction`/`rejectPermintaanNikAction` (Super Admin/Operator), divalidasi ulang NIK belum dipakai anggota lain sebelum disetujui. Kartu "PERMINTAAN PERUBAHAN DATA" ditambahkan ke drawer CV Anggota yang sudah ada sejak Fase 5 (bukan halaman baru terpisah) — approval terjadi di tempat yang sudah Operator biasa buka
- **Deviasi disengaja dari mockup, didokumentasikan supaya tidak dikira lupa**: Nama Lengkap & Unit Asal dikunci read-only di form Profil (mockup membiarkannya bebas diedit — dianggap terlalu longgar untuk identitas inti personel cadangan); Kompetensi & Spesialisasi read-only dari Sertifikasi resmi, bukan chip bebas-tambah (konsisten dengan keputusan Fase 5); tombol unggah foto jujur bilang "belum tersedia" alih-alih toast simulasi sukses palsu seperti mockup; menu bottom-nav dipangkas dari 5 ke 4 (drop "Pengaturan" yang tidak ada baik di mockup maupun cakupan FR-37 s.d. FR-40)
- **Bug nyata ditemukan & diperbaiki**: `loginAction` hardcode `redirectTo: "/overview"` untuk semua role, mengandalkan `proxy.ts` membelokkan ANGGOTA ke `/m` di request berikutnya. Ternyata redirect dari dalam Server Action adalah client-side transition yang TIDAK memicu ulang middleware secara andal — dikonfirmasi dengan raw `fetch` (redirect manual) yang menunjukkan proxy.ts SEBENARNYA benar (307 ke `/m`) kalau diakses langsung, tapi jalur signIn→redirect tidak melewati jalur itu dengan cara yang sama. Diperbaiki dengan menentukan tujuan redirect dari role sungguhan (lookup email) DI DALAM `loginAction`, sebelum memanggil `signIn()` — tidak lagi bergantung pada bounce-back middleware sama sekali. Ini adalah bug yang sudah ada sejak Fase 2 tapi baru ketahuan sekarang karena Fase 2 diverifikasi pakai curl+cookie manual (melewati jalur Server Action redirect), bukan lewat klik form sungguhan sebagai Anggota — pelajaran: curl test bisa melewatkan bug yang cuma muncul di jalur UI sungguhan
- **Verifikasi Playwright lintas dua sisi** (bukan cuma satu peran): login Anggota (viewport 430px, mobile) → ubah status Siaga → ubah profil (NIK + pekerjaan sipil) → simpan → pesan "menunggu persetujuan Admin" tampil → 3 tab Riwayat berpindah tanpa error → respons notifikasi Konfirmasi → **ganti browser context, login Operator** (viewport desktop) → buka Manajemen Misi, kehadiran personel itu sudah "Dikonfirmasi" (bukti nyata loop nyambung, bukan asumsi) → Direktori Anggota, status Siaga tersinkron → buka drawer CV, kartu Permintaan Perubahan Data tampil → Setujui → **NIK di database benar-benar berubah** (dikonfirmasi query Prisma langsung, bukan cuma baca UI). Nol console error di kedua sisi. Data uji (termasuk 1 Penugasan+Notifikasi yang sengaja dibuat manual lewat script untuk memicu skenario respons notifikasi, karena seed default tidak memberi notifikasi ke akun demo Anggota) dibersihkan, dev DB direset & di-reseed ulang

**Fase 12 — Non-Functional Requirements (selesai):**
- `src/lib/crypto.ts` — `encryptSensitive`/`decryptSensitive` (AES-256-GCM, IV acak per baris, `ENCRYPTION_KEY` 32-byte hex dari env) dan `hashSensitive` (HMAC-SHA256 deterministik) untuk NFR-04. `Anggota.nik` sekarang tersimpan ciphertext, kolom baru `Anggota.nikHash` (HMAC, `@unique`) menggantikan peran `nik` lama sebagai kolom lookup/cek-duplikat — karena IV acak bikin dua ciphertext dari NIK yang sama tidak pernah identik, jadi `@unique` di kolom terenkripsi langsung mustahil. `PermintaanUbahData.nilaiLama/nilaiBaru` ikut dienkripsi kalau `field === "nik"`. Titik dekripsi cuma di data-fetcher server yang sudah digerbangi RBAC (`anggota-data.ts`, `anggota-mobile-data.ts`) — komponen di bawahnya tetap terima nilai plaintext seperti sebelumnya, tidak ada perubahan kontrak data ke UI
- `src/lib/prisma.ts` — NFR-08: `$extends` query middleware yang melempar error untuk `auditLog.update/updateMany/delete/deleteMany/upsert`, `create` tetap normal. Ini pertahanan di level Prisma Client itu sendiri, bukan cuma disiplin "kode kita tidak pernah memanggilnya"
- `src/lib/notifikasi-delivery.ts` — NFR-07: `deliverNotifikasiBatch()` mengirim tiap Notifikasi independen dengan retry (maks 2x), fallback ke `channel: "SMS (Simulasi)"` + `status: "Gagal Terkirim - Fallback"` kalau tetap gagal, dipakai `approveMisiAction` menggantikan `notifikasi.createMany` di dalam transaksi tunggal — sekarang status Misi (pasti berhasil) dan pengiriman notifikasi (best-effort per kandidat) dipisah, supaya satu kandidat gagal tidak lagi bisa menggagalkan seluruh approval Misi
- Aksesibilitas §10.8: tombol "−" sembunyikan panel Overview (`bottom-panel-shell.tsx`) dinaikkan 20px→30px + `aria-label`; token `--ink-3` di `globals.css` dinaikkan `#565f67`→`#70787f` (kontras dihitung manual ~3.2:1→~4.7:1 terhadap `--base`/`--surface`, dipakai di 31 file untuk teks sekunder)
- **Bug regresi nyata ditemukan & diperbaiki di tengah fase ini**: begitu `prisma.ts` ditambah `$extends`, Turbopack berhenti men-tree-shake modul itu keluar dari bundle client di `/anggota` — ternyata `anggota-cv-drawer-content.tsx` dan `profil-view.tsx` (keduanya `"use client"`) selama ini ikut menarik seluruh `anggota-data.ts`/`anggota-mobile-data.ts` (yang meng-`import { prisma }`) ke bundle browser lewat satu function import biasa (`calcUsia`) yang bukan `import type`. Prisma Client sendiri melempar "unable to run in this browser environment" saat modul itu dieval di browser — Direktori Anggota jadi blank. Kemungkinan besar celah ini SUDAH ADA sejak Fase 5, cuma kebetulan sebelumnya bundler berhasil men-tree-shake export `prisma` yang tidak dipakai `calcUsia`; begitu `prisma.ts` punya bentuk statement yang sedikit berbeda (`$extends(...)` bukan `new PrismaClient()` polos), heuristik DCE Turbopack berhenti berhasil. Diperbaiki dengan memindahkan `calcUsia` ke module murni baru `src/lib/usia.ts` (nol dependensi server) dan mengubah kedua client component itu jadi cuma `import type` dari module data-fetcher — perbaikan struktural, bukan cuma menghindari gejala. **Pelajaran untuk fase berikutnya**: client component TIDAK BOLEH mengimpor apa pun secara value-level (non-`type`) dari file yang isinya query Prisma, walau fungsi yang diimpor itu sendiri tidak menyentuh DB — taruh util murni di file terpisah dari file data-fetcher
- Diverifikasi: query SQLite mentah membuktikan `nik` tersimpan ciphertext (bukan 16 digit) sebelum & sesudah alur approval; script langsung mencoba `auditLog.update`/`delete` dan keduanya terblokir sementara `create` tetap jalan; alur Sisi Anggota ajukan NIK baru → Operator lihat nilai lama/baru yang sudah didekripsi benar di drawer CV → Setujui → NIK di DB berubah (tetap terenkripsi) diverifikasi Playwright lintas dua konteks browser; `deliverNotifikasiBatch` diuji langsung ke data seed asli (`channel: "Aplikasi"`, `status: "Terkirim"`, `deliveryAttempts: 1` — jalur fallback sendiri defensif untuk produksi, tidak bisa didemonstrasikan hidup di dev/SQLite tanpa fault injection, didokumentasikan jujur sebagai itu); 13 route Command Center dibuka satu-satu, nol console error; waktu Overview (klik login → peta Leaflet visible) ~1.8 detik warm. Data uji dibersihkan, dev DB direset & di-reseed ulang

**Fase 13 — Testing, QA, Deployment Prep (selesai — semua 13 fase rencana awal tuntas):**
- Smoke test lintas 4 role (Super Admin/Operator/Analis/Anggota) × 13 route Command Center + 4 layar Sisi Anggota via satu script Playwright: login tiap role → kunjungi semua route relevan → nol console error di seluruhnya. Batas RBAC diverifikasi dengan bukti nyata, bukan dibaca dari kode saja: `/pengguna` & `/pengaturan` menampilkan pesan akses-ditolak (bukan crash/blank) untuk Operator & Analis; tombol "BUAT MISI" hilang dari topbar untuk Analis; Anggota yang mengetik URL Command Center (`/anggota`) langsung dibelokkan balik ke `/m` oleh `proxy.ts`; halaman Laporan menampilkan teks pembatasan unduh yang sesuai per role
- Alur end-to-end §14.1 diverifikasi sebagai SATU rantai Playwright tunggal dari awal sampai akhir (bukan potongan-potongan test per fase yang digabung secara asumsi): Buat Misi → Generate Rekomendasi AI (OpenAI beneran terpanggil) → Setujui & Kirim Notifikasi → kode Misi baru dicek MUNCUL di tabel Manajemen Misi dengan status Dimobilisasi → dicek MUNCUL juga di panel Overview → Tutup Misi & Evaluasi → status berubah Selesai di drawer → dicek MUNCUL di tabel Riwayat Mobilisasi. Nol console error sepanjang rantai
- `app/README.md` dibuat dari nol — bukan salinan `CLAUDE.md` (yang isinya keputusan arsitektur & histori), README fokus ke "cara jalanin & kembangin": setup pertama kali, akun demo, daftar perintah lengkap termasuk cara reset DB penuh, pola testing manual (dicatat jujur: tidak ada Jest/Vitest, verifikasi pakai script Playwright sekali-pakai — bukan pura-pura ada test suite permanen), struktur folder, tabel environment variable, dan **aturan client-bundle Prisma** (pelajaran dari bug regresi Fase 12) ditulis eksplisit supaya developer baru/sesi AI berikutnya tidak mengulanginya
- Catatan deployment ditulis lengkap: migrasi SQLite→Postgres langkah demi langkah (`prisma migrate deploy`, bukan `db push`, untuk prod), checklist sebelum rilis, dan peringatan soal rotasi `ENCRYPTION_KEY` (NIK yang sudah terenkripsi dengan key lama butuh proses decrypt-lalu-encrypt-ulang sebelum key lama dibuang, bukan sekadar ganti env var). **Target hosting ditanyakan eksplisit ke user** (bukan diasumsikan Vercel demi kecepatan) — user memilih tetap TBD, didokumentasikan sebagai keputusan sensitif keamanan nasional yang perlu proses/otorisasi terpisah dari sesi coding
- Data uji dari alur E2E (1 Misi `MISI-2026-003` berstatus Selesai + Notifikasi terkait) dibersihkan, dev DB direset & di-reseed ulang ke starter set bersih sebelum commit

**Fase 14 — Penyempurnaan Gap Teknis (selesai, di luar 13 fase rencana awal):**
- User diminta memilih dari daftar "Simplifikasi/gap teknis" yang selama ini didokumentasikan honestly per fase — 7 item dipilih lewat `AskUserQuestion`, bukan diasumsikan sendiri mana yang prioritas
- `src/lib/readiness.ts` — `computeReadinessScore()` (pure function, formula ASUMSI: Kompetensi 40%/Pelatihan 30%/Penugasan 30%) + `recalculateReadinessScore(anggotaId)` (DB read-compute-write). Dipanggil dari 3 titik mutasi riwayat kehadiran: `closeMisiAction`, `updateKehadiranAction` (Command Center), `respondNotifikasiAction` (self Sisi Anggota) — Readiness Score sekarang benar-benar bereaksi ke aktivitas anggota, bukan angka statis dari seed
- `src/lib/reminder-sertifikasi.ts` — `ensureReminderSertifikasi(anggotaId?)`, idempotent (judul Notifikasi menyertakan tanggal berlaku supaya satu siklus kedaluwarsa cuma diingatkan sekali). Dipanggil dari `app/m/layout.tsx` (diri sendiri) dan `app/(command)/overview/page.tsx` (semua anggota) — tidak ada cron persisten di lingkungan ini, jadi "terjadwal" di sini artinya "dicek ulang tiap kali ada yang membuka halaman relevan", bukan job background sungguhan
- `situation-map.tsx` — `computeHeatBins()`: kepadatan anggota per grid ~1° dihitung di client dari data yang sudah dimuat (tanpa query/library tambahan), divisualisasikan sebagai `Circle` Leaflet dengan radius/warna/opacity mengikuti jumlah anggota per bin. Toggle "Kepadatan Wilayah" baru di `LayersPanel`, default state mengikuti `PengaturanSistem.petaHeatzone`
- `src/lib/overview-actions.ts` (baru) + `getAnggotaDetail`/`getMisiDetail` yang sudah ada dipakai ulang: klik marker anggota/zona Misi di Overview sekarang fetch on-demand lalu render `AnggotaCvDrawerContent`/`MisiDetailDrawerContent` yang SAMA PERSIS dengan Direktori Anggota/Manajemen Misi — bukan versi ringkas 4-field terpisah yang ada sejak Fase 4. `onEdit` dari drawer Overview mengarahkan ke `/anggota` (bukan duplikasi modal edit) — trade-off sadar supaya tidak menggandakan state management edit di dua tempat
- `src/lib/geocoding.ts` — `geocodeAlamat()` pakai Nominatim (OpenStreetMap, gratis, tanpa API key, satu ekosistem dengan tile peta). Dipanggil lewat Server Action `geocodeLokasiAction` per klik tombol (bukan live-search), sesuai kebijakan penggunaan Nominatim (maks ~1 request/detik, User-Agent jelas). Hasil divalidasi dalam batas geografis Indonesia. Form Buat Misi jadi hybrid: input alamat bebas teks + tombol "Cari Lokasi", DENGAN dropdown Lokasi Referensi tetap ada sebagai fallback tanpa dependensi jaringan — `buatMisiSchema` (`misi-actions.ts`) diubah dari `lokasiKey` (enum) jadi `{lokasiLabel, lokasiLat, lokasiLng}` (bebas, divalidasi ulang batas Indonesia di server sebagai defense-in-depth)
- `prisma/seed.ts` — 20→60 anggota, 8→12 provinsi, dan pass akhir memanggil `computeReadinessScore()` sungguhan per anggota dari sertifikasi/pelatihan/penugasan yang baru di-seed (bukan lagi formula acak `55 + (i*7)%45`)
- Interval polling auto-refresh Overview: 15 detik → 5 detik (lebih dekat ke target FR-07 ≤5 detik, tetap request-based bukan push sungguhan)
- **Bug nyata ditemukan & diperbaiki selama verifikasi Playwright**: `POS_KOMANDO` (`lib/pos-komando.ts`) — 3 dari 4 koordinatnya pixel-identik dengan kota provinsi seed (Bandung, Medan, Makassar), 1 lagi sangat dekat Jakarta. Karena Leaflet selalu merender `markerPane` (tempat Marker/divIcon seperti Pos Komando) DI ATAS `overlayPane` (tempat CircleMarker/Circle seperti anggota & zona Misi), marker Pos Komando menutupi & memblokir klik marker anggota di kota-kota itu — bahkan di zoom nasional (default Overview), bukan cuma saat zoom dekat. Diperbaiki dengan menggeser koordinat Pos Komando ~1-1.5° dari kota provinsi manapun (tetap di region yang sama secara konsep)
- **Pelajaran metodologi testing** (bukan bug aplikasi, dicatat supaya tidak salah diagnosis lagi): di zoom nasional dengan puluhan marker padat, Playwright `locator.click()` (mode strict, tanpa `force`) BENAR menolak mengklik kalau ada elemen lain (marker berdekatan lain, termasuk marker anggota lain yang render di pixel hampir sama) menutupi target — ini realistis untuk skenario "user perlu zoom in dulu", TAPI kalau tujuan verifikasi cuma "drawer bisa kebuka", `force: true` (yang tetap memicu event klik asli di koordinat itu, hit-test browser sungguhan tetap berlaku) sudah cukup representatif — SELAMA halaman sudah benar-benar selesai `fitBounds`/animasi awal (perlu wait eksplisit ~3 detik setelah `.leaflet-container` muncul, bukan cuma `waitForSelector` itu sendiri) sebelum klik, kalau tidak posisi marker yang diukur `boundingBox()` sudah basi saat klik dieksekusi
- Diverifikasi Playwright end-to-end: 60 baris Direktori Anggota, toggle heat-zone 2× nol error, klik marker anggota → drawer berisi "Sertifikasi"/"Riwayat Pelatihan"/"UBAH STATUS KESIAPAN" (bukti CV lengkap), klik zona Misi → drawer berisi "Pemantauan Kehadiran" (bukti detail lengkap), geocoding "Kabupaten Garut, Jawa Barat" → lokasi+koordinat nyata muncul, dropdown fallback tetap berfungsi, **Misi baru dibuat penuh pakai lokasi geocoding "Kabupaten Cianjur, Jawa Barat"** → dikonfirmasi query DB langsung (`latitude`/`longitude` asli, 3 kandidat + ETA terhitung). Nol console error di semua langkah. Data uji dibersihkan, dev DB direset & di-reseed ulang (60 anggota bersih)

## Cara Login untuk Testing Manual

Jalankan `npm run dev` dari `app/`, buka `http://127.0.0.1:3000` (lihat catatan `localhost` vs `127.0.0.1` di bawah), lalu masuk dengan salah satu akun (password sama semua): `komcad123`

| Email | Role | Diarahkan ke |
|---|---|---|
| admin@komcad.mil.id | Super Admin | Command Center |
| operator@komcad.mil.id | Operator Komcad | Command Center |
| analis@komcad.mil.id | Analis/Evaluator | Command Center |
| anggota@komcad.mil.id | Anggota Komcad | Sisi Anggota (`/m`) |

## Catatan Lingkungan Dev (penting untuk sesi lanjutan)

Mesin dev ini **tidak** punya Node.js/npm bawaan dan **tidak** ada akses admin (MSI installer Node gagal — Error 1925 insufficient privileges). Solusinya:
- Node.js v24.19.0 diinstal sebagai **portable zip** ke `%LOCALAPPDATA%\Programs\node-v24.19.0-win-x64` (bukan via installer resmi)
- Ditambahkan ke **User PATH** (registry) — berlaku untuk sesi shell baru
- Untuk Git Bash yang sesi-nya sudah berjalan sebelum PATH diubah: dibuat wrapper `node`/`npm`/`npx` (bash script) di `~/bin` — folder ini sudah ada di PATH bawaan Git Bash
- **Penting:** subprocess native Windows (cmd.exe, yang dipanggil banyak install script npm seperti Prisma postinstall) butuh file `.cmd` asli, bukan bash script. Maka dibuat juga `node.cmd`, `npm.cmd`, `npx.cmd` di `~/bin` yang memanggil `node.exe` asli dengan path absolut — tanpa ini, `npm install` gagal dengan `'node' is not recognized` setiap kali sebuah package menjalankan install script.
- Kalau lingkungan dev berubah (mesin baru / container baru), langkah instalasi Node portable + shim `.cmd` ini perlu diulang kalau tidak ada akses admin untuk installer resmi.
- npm 11+ punya fitur `allow-scripts` (block install script secara default) — kalau ada package baru yang install-nya gagal diam-diam, cek `npm approve-scripts --allow-scripts-pending` dulu.
- **`localhost` sempat tidak resolve** di dalam sesi kerja ini tapi `127.0.0.1` selalu jalan — kalau curl/browser gagal connect ke `localhost:3000`, coba `127.0.0.1:3000`.
- Git Bash + `cmd.exe /c "..."` sering memangkas argumen `/c` jadi path (MSYS path-mangling) — pakai `cmd.exe //c "..."` (garis miring ganda) kalau perlu menjalankan command lewat cmd.exe dari Git Bash.

## Keputusan Teknis yang Diambil Selama Sesi Ini

- **Prisma versi**: dipin ke `^6` (bukan v7 terbaru) karena Prisma 7 mengubah cara konfigurasi datasource (butuh `prisma.config.ts` + driver adapter, breaking change besar). v6 tetap pakai `url` di `schema.prisma` seperti biasa.
- **Tailwind v4**: dipakai apa adanya dari default `create-next-app` — theming lewat `@theme` block di CSS, bukan `tailwind.config.js`.
- **Next.js 16 "proxy" bukan "middleware"**: file route-protection ada di `src/proxy.ts`, bukan `src/middleware.ts` (nama lama dideprecate mulai Next 16.0.0).
- **Login pakai Server Action, bukan client `signIn()`**: `src/app/login/actions.ts` memanggil `signIn` dari `@/lib/auth` (server-side), ditangkap lewat `useActionState`. Ini menghindari perlu `"use client"` untuk seluruh logic auth dan `AuthError` ditangani rapi tanpa expose detail error ke user.
- **RBAC di level proxy (route), bukan cuma UI**: halaman Command Center & Sisi Anggota benar-benar saling terkunci di server (redirect), bukan cuma sembunyi-tampil elemen di client. Pola yang sama dilanjutkan di level Server Action untuk operasi CRUD (Fase 5) — jangan pernah cuma andalkan sembunyi tombol di UI untuk otorisasi.
- **lucide-react tidak lagi punya ikon brand** (Instagram, LinkedIn, dll — versi 1.31.0 yang terpasang). Kalau butuh ikon sosial media lagi di modul lain, pakai ikon generik (`AtSign`, `Link2`, dll) seperti di CV drawer, bukan asumsi nama ikon brand tersedia.
- **`key` prop untuk memaksa remount komponen form yang dipakai ulang untuk create/edit**: kalau ada component dengan `useActionState` yang di-reuse untuk beberapa "target" berbeda (create vs edit-anggota-X vs edit-anggota-Y) tanpa pernah unmount, action binding bisa nyangkut ke target lama. Di Fase 6, `BuatMisiModal` dihindarkan dari masalah ini karena selalu single global instance (bukan di-reuse per-record) dan wizard state di-reset manual saat modal ditutup — tapi kalau nanti ada form Misi yang di-reuse per-record (mis. edit Misi), ingat pola ini.
- **Server Action dipanggil langsung dari client (bukan via `useActionState`+form)** untuk alur multi-step: `generateMisiAction`/`approveMisiAction`/`closeMisiAction` dipanggil sebagai fungsi async biasa di dalam `startTransition`, bukan di-bind ke `<form action>`. Ini valid untuk fungsi `"use server"` yang diimpor ke client component, dan lebih cocok untuk wizard (form → loading → result → approve) dibanding `useActionState` yang mengasumsikan satu form dengan satu hasil.
- **Prisma destructive command (`db push --force-reset`) diblokir otomatis** oleh guard Claude Code sendiri kalau dijalankan tanpa konfirmasi user eksplisit di pesan itu — ini best-practice yang sengaja tidak dilewati (bukan di-bypass pakai flag tersembunyi). Kalau perlu reset dev DB lagi nanti, minta izin dulu ke user sebelum menjalankan `PRISMA_USER_CONSENT_FOR_DANGEROUS_AI_ACTION=... npx prisma db push --force-reset`.
- **OpenAI structured output (`response_format: json_schema` + `enum` pada field)** efektif untuk grounding: dengan meng-*enum*-kan `anggotaId` ke daftar kandidat asli yang dikirim, model API secara struktural tidak bisa mengembalikan personel di luar itu — divalidasi ulang juga di kode (`validIds.has(...)`) sebagai defense-in-depth, bukan cuma percaya skema.
- **`serverExternalPackages` di `next.config.ts`** wajib untuk paket Node yang membaca asset (font, native binding, dsb) relatif ke `__dirname`-nya sendiri saat runtime (`pdfkit` contoh nyata di Fase 7) — kalau di-bundle Turbopack/webpack, `__dirname` berubah jadi path virtual dan asset itu tidak ketemu (`ENOENT`). Cek dulu paket serupa (`canvas`, native addon apa pun) kalau muncul gejala sama di fase berikutnya.
- **Route Handler (`app/api/.../route.ts`) untuk unduhan file**: dipakai untuk laporan PDF/XLSX supaya browser bisa native-download via `Content-Disposition: attachment`. Link `<a href>` biasa (bukan `router.push`) atau `window.location.href` di client dipakai untuk memicunya — `router.push` tidak akan memicu dialog unduh karena itu navigasi client-side RSC, bukan request browser penuh.
- **`signIn(..., {redirectTo})` di Server Action jangan hardcode satu tujuan untuk semua role** — redirect dari dalam Server Action adalah client-side transition yang ternyata tidak memicu ulang `proxy.ts` secara andal di lingkungan ini (ditemukan di Fase 11). Tentukan tujuan redirect dari data sungguhan (role) SEBELUM memanggil `signIn()`, jangan berharap middleware "membenarkan" tujuan yang salah di request berikutnya.
- **Prisma: field `xxxId String?` polos BUKAN relasi** — kalau mau `include`/`select` relasi itu di query, harus eksplisit tambah `xxx Model? @relation(fields:[xxxId], references:[id])` plus array balik di model lawannya. `Notifikasi.misiId` sudah ada sejak Fase 6 tapi baru punya relasi asli di Fase 11 saat benar-benar butuh `include: {misi:true}` — sebelum itu field-nya cuma string biasa yang kebetulan berisi id valid.
- **Field terenkripsi tidak bisa `@unique` langsung** (Fase 12, NIK): kalau sebuah kolom butuh enkripsi at-rest DAN kolom itu juga jadi kunci pencarian/unik, jangan enkripsi kolom yang sama — tambah kolom hash deterministik terpisah (HMAC, bukan enkripsi acak) khusus untuk lookup/`@unique`, simpan nilai terenkripsi di kolom aslinya untuk ditampilkan lagi. Dua kolom, dua tujuan berbeda: satu bisa dibalik (untuk dilihat user berwenang), satu tidak (cuma untuk dibandingkan).
- **Client component tidak boleh value-import dari file data-fetcher Prisma**, walau cuma untuk satu fungsi murni di dalamnya — modul JS dieksekusi utuh saat diimpor, jadi `import { prisma }` di baris atas ikut jalan di bundle browser dan Prisma Client sendiri akan melempar error di sana. Ini nyaris jadi bug tersembunyi di Fase 5 dan baru pecah di Fase 12 gara-gara perubahan kecil di `prisma.ts` mengubah hasil tree-shaking bundler. Aturan aman: dari client component, HANYA `import type` dari file data-fetcher; util murni yang dipakai bersama (kayak `calcUsia`) harus tinggal di file terpisah tanpa dependensi server sama sekali.
- **Leaflet `markerPane` (Marker/divIcon) SELALU di atas `overlayPane` (CircleMarker/Circle/Polygon)** — ini z-index bawaan Leaflet (600 vs 400), bukan sesuatu yang bisa diatur lewat prop React biasa. Kalau ada dua layer data yang koordinatnya bisa berdekatan/bertabrakan (Fase 14: Pos Komando pakai Marker, anggota pakai CircleMarker), Marker akan selalu "menang" secara klik walau CircleMarker terlihat di atasnya secara visual pada zoom tertentu. Solusinya bukan CSS/z-index — jauhkan koordinatnya, atau pakai custom pane kalau memang perlu urutan berbeda.
- **Overlay dekoratif yang menutupi seluruh viewport WAJIB `pointer-events: none`** (Fase 15). Tekstur global HUD (`body::before/::after`) berada di `z-index` 9998/9999, jauh di atas modal & drawer Radix (2000/2001) — tanpa `pointer-events: none`, seluruh aplikasi jadi tidak bisa diklik sama sekali dan gejalanya terlihat seperti "semua tombol rusak", bukan seperti masalah CSS. Berlaku untuk overlay dekoratif apa pun yang ditambahkan nanti.
- **Tema visual ditambahkan sebagai lapisan, bukan sebagai pengganti token** (Fase 15). FRD §10.2 mengunci palet, jadi Sentinel HUD dibangun murni dari utility tambahan di `globals.css` (gradien tipis, glow, tekstur, corner bracket) tanpa mengubah satu pun nilai hex. Konsekuensi praktisnya: kalau nanti diminta ganti tema lagi, yang dibongkar cukup blok "SENTINEL HUD" di `globals.css` + segelintir kelas di komponen shell — bukan mengaudit ulang seluruh warna aplikasi.
- **Untuk perubahan gaya yang polanya identik di banyak file** (Fase 15: 7 file tabel dengan string kelas yang persis sama), penggantian mekanis lewat script sekali-pakai lebih aman daripada belasan edit manual — asalkan langsung diikuti `tsc` + `lint` + screenshot verifikasi. Yang berbahaya bukan script-nya, tapi menganggap "sudah diganti" tanpa melihat hasilnya.
- **Verifikasi fungsional tidak menangkap bug tata letak** (Fase 15): kontrol zoom Leaflet tertimbun panel Layers sejak Fase 4 dan lolos dari semua smoke test Fase 4–14, karena test-nya selalu bertanya "ada console error?" dan "handler-nya jalan?", tidak pernah "apakah ini kelihatan benar?". Screenshot yang benar-benar dibuka dan dilihat menemukannya dalam sekali lihat.
- **Playwright `locator.click()` (strict, tanpa `force`) menolak mengklik target yang sedang tertutup elemen lain** — di peta padat (banyak marker berdekatan di zoom rendah), ini SERING kejadian dan valid (mencerminkan kebutuhan user untuk zoom in dulu), tapi bisa bikin verifikasi false-negative kalau tujuannya cuma "pastikan handler klik jalan". `force: true` lebih representatif untuk itu — tapi WAJIB disertai wait yang cukup (~3 detik) setelah peta pertama muncul supaya `fitBounds`/animasi selesai dulu, kalau tidak posisi yang diukur `boundingBox()` sudah basi saat klik dieksekusi dan malah salah sasaran.

## Fase 15 — Rebrand SIAGA + Tema Visual Sentinel HUD

Fase ini dipicu penilaian user bahwa tampilan sebelumnya "basic, plain, dan membosankan". Yang diminta: 3 konsep desain dengan arah visual berbeda, semuanya tetap comply penuh ke FRD, untuk dipilih user — bukan satu redesign yang langsung diterapkan.

**Tiga konsep, bukan tiga tema warna.** Ketiganya dibuat sebagai HTML mandiri di `mockup-konsep/`, masing-masing meng-cover seluruh permukaan FRD (13 menu × 5 grup nav, topbar lengkap, peta situasi + 4 overlay, 3 panel bawah + show/hide, drawer CV, drawer Misi, modal Buat Misi 4-state, AI Chat, semua halaman tabel). Peta Leaflet + tile OSM sungguhan di ketiganya supaya perbandingannya jujur, bukan gambar peta yang di-mock. Diverifikasi Playwright: nol console error, semua interaksi (klik marker → drawer, modal 4-state, sembunyikan panel → chip restore, pindah menu) benar-benar jalan di ketiganya.
- **Sentinel HUD** — kokpit tempur. **Dipilih user.** Palet FRD apa adanya, ditambah kedalaman: grid halus + vignette + scanline, corner bracket, glow tactical green, crosshair peta
- **Garuda Prestige** — ruang briefing kenegaraan. Emas kuningan + gading, serif display (Fraunces), peta sepia berbingkai + mawar kompas, banyak ruang napas
- **Nusantara Ops** — konsol lapangan neo-brutalist. Radius 0 di mana-mana, border 2px, tombol hard-shadow, monospace dominan, peta fosfor hijau kontras tinggi

Dua konsep yang ditolak **tetap disimpan di repo** sebagai catatan keputusan desain, bukan dihapus.

**Radar sweep dihapus atas permintaan eksplisit user.** Konsep Sentinel awalnya punya radar sweep berputar 9 detik di atas peta. User suka konsepnya tapi minta sweep-nya tidak ada. Dihapus dari file konsepnya juga, bukan cuma tidak diimplementasikan ke app — supaya file konsep tetap merepresentasikan apa yang benar-benar disetujui. Alasan teknisnya juga masuk akal dan ditulis di komentar CSS: animasi rotasi terus-menerus di atas layer marker mengganggu pembacaan sebaran dan tidak menambah informasi apa pun.

**Nama produk: AI KOMCAD → SIAGA.** User minta 10 ide nama yang berupa kata bermakna sekaligus akronim, memilih SIAGA, lalu minta 10 variasi kepanjangan dan memilih *Sistem Identifikasi, Analitik & Gerak Anggota*. Kata "Komcad" **sengaja tidak ikut diganti** di mana pun ia merujuk program/entitas domain (Anggota Komcad, Operator Komcad, domain email `@komcad.mil.id`, nama unit "Komcad Yon Zeni 1") — nama produk dan nama program itu dua hal berbeda, dan cari-ganti buta akan merusak istilah domain yang dipakai FRD. Aturan ini ditulis eksplisit di `CLAUDE.md` dan `app/README.md` supaya sesi berikutnya tidak salah paham.

**Penerapan tema: global, lewat pondasi CSS dulu, baru komponen.** Pilihan user adalah "global semua halaman", jadi mengedit 60+ komponen satu per satu bukan pendekatan yang masuk akal. Yang dilakukan: taruh chrome HUD sebagai utility di `globals.css` (`.hud-brk` corner bracket, `.hud-panel` gradien permukaan, `.hud-head` sheen header, `.hud-rule-x/y` garis aksen gradien, `.hud-num` glow angka instrumen, `.hud-reticle` crosshair peta, `.hud-label` hairline memudar), plus dua overlay tekstur di `body::before/::after` yang otomatis kena ke seluruh aplikasi tanpa menyentuh satu komponen pun. Setelah itu baru komponen kunci dipasangi utility tadi: sidebar, topbar, `Card`, `BottomPanelShell`, tiga panel overlay peta, `Modal`, `Drawer`, `Button` varian solid, kartu KPI Analitik, panel Statistik Anggota. Untuk 7 file tabel yang polanya identik, penggantian kelas dilakukan mekanis lewat script sekali-pakai lalu diverifikasi `tsc` + `lint` + screenshot — bukan 14 edit manual yang rawan luput satu.

**Nol nilai hex FRD §10.2 diubah.** Ini batasan yang dipegang sepanjang fase: FRD mengunci palet, jadi yang boleh bertambah cuma kedalaman (gradien tipis, glow, tekstur, bracket), bukan warna baru. Ditulis sebagai komentar di kepala `globals.css` supaya jelas untuk siapa pun yang membaca file itu nanti.

**Dua jebakan aksesibilitas yang sengaja ditangani di depan**, bukan ditemukan sebagai bug nanti:
1. Kedua overlay tekstur menutupi seluruh viewport di `z-index` 9998/9999 — **di atas** modal & drawer Radix (2000/2001). Tanpa `pointer-events: none` di keduanya, seluruh aplikasi jadi tidak bisa diklik sama sekali. Ditulis eksplisit sebagai peringatan di komentar CSS.
2. `@media (prefers-reduced-motion: reduce)` mematikan scanline dan meredam grid — pengguna yang minta pengurangan efek tetap dapat aplikasi yang terbaca, bukan tekstur berlapis yang sama.

**Bug pra-existing ditemukan lewat screenshot verifikasi:** kontrol zoom Leaflet memakai posisi bawaan kiri-atas, persis di belakang panel Layers di Overview — jadi tombol +/− praktis tidak bisa dipakai sejak Fase 4 dan tidak pernah ketahuan karena verifikasi sebelumnya fokus ke fungsi, bukan ke tata letak visual. Dipindah ke kanan-bawah (`ZoomControl position="bottomright"` + `zoomControl={false}` di `MapContainer`) dan digayakan mengikuti chrome HUD.

**Kepanjangan akronim ditampilkan permanen, bukan disembunyikan di tooltip.** Setelah rebrand, muncul pertanyaan wajar: "SIAGA" itu singkatan, tapi kepanjangannya tidak kelihatan di mana pun kecuali halaman login. Tiga opsi ditawarkan ke user (baris permanen di bawah wordmark / hover-reveal / footer sidebar) dan user memilih baris permanen. Alasan opsi hover-reveal tidak direkomendasikan meski paling "bersih": tidak terlihat di layar sentuh dan tidak muncul di screenshot demo ke pemangku kepentingan — dua konteks yang justru paling butuh penjelasan nama.

Solusinya `components/shell/brand-tagline.tsx`: kepanjangan ditulis penuh di 8px, dengan huruf pembentuk akronim **S-I-A-G-A diwarnai accent**. Ini yang membuatnya tetap sleek — mata langsung menangkap hubungan huruf-ke-wordmark tanpa perlu teks penjelas seperti "singkatan dari", dan kalimatnya terbaca sebagai tekstur desain, bukan sebagai keterangan yang menambah berat. Ternyata muat **satu baris** di kedua permukaan (sidebar 236px dan header mobile 430px), jadi tambahan tingginya cuma ~14px, bukan tiga baris seperti perkiraan awal.

Subteks **"COMMAND CENTER" sengaja dipertahankan** — FRD §10.1 mengunci subteks itu di bawah wordmark, jadi kepanjangan ditambahkan sebagai baris terpisah di bawah hairline, bukan menggantikannya. Ini dikonfirmasi ke user sebelum memilih, bukan diputuskan diam-diam.

Pecahan per huruf ditulis eksplisit sebagai array pasangan di `lib/constants.ts` (`PRODUK_KEPANJANGAN_SEGMEN`), bukan hasil regex atas string kepanjangan. Regex akan diam-diam salah warna kalau kalimatnya nanti diubah; array eksplisit memaksa perubahan itu dilakukan sadar di dua tempat sekaligus (dan komentarnya menyebut kewajiban itu).

Selain sidebar, user juga memilih memasangnya di **header Sisi Anggota mobile** dan **header laporan PDF/XLSX**. Yang terakhir punya alasan berbeda: laporan keluar dari sistem dan dibaca pihak luar yang belum tentu kenal akronimnya, jadi di sana nama produk selalu ditulis lengkap dengan kepanjangannya.

**Verifikasi:** `tsc --noEmit`, `npm run lint`, dan `npm run build` bersih. Screenshot Playwright untuk login, Overview, Direktori Anggota, Analitik, Manajemen Misi, Pengaturan, drawer CV, dan Sisi Anggota mobile (viewport 430×880) — nol console error di semuanya. Screenshot benar-benar dilihat satu per satu, bukan cuma dicek "tidak error". Header laporan PDF diverifikasi dengan mendekompresi content stream dari file hasil unduhan sungguhan (baris 1 `SIAGA`, baris 2 kepanjangan penuh) — bukan diasumsikan dari kode.

## Status: Semua 13 Fase Rencana Awal + Fase 14 & 15 (Penyempurnaan) Selesai

Platform sudah lengkap secara fungsional sesuai `TODO.md` dari Fase 0 sampai Fase 13, ditambah 7 item penyempurnaan gap teknis di Fase 14 dan rebrand + tema visual di Fase 15 — keduanya dipilih langsung oleh user, bukan diasumsikan. Yang tersisa BUKAN fitur yang belum dibangun, tapi keputusan bisnis/skala yang sengaja tidak diasumsikan sepihak (lihat bagian "Backlog / Perlu Klarifikasi User" di `TODO.md`) dan simplifikasi minor yang masih terdokumentasi jujur per fase di atas (mis. drawer edit Anggota dari Overview mengarahkan ke `/anggota` alih-alih modal terpisah, reminder sertifikasi & heat-zone masih on-demand bukan cron/library heatmap sungguhan).

## Rekomendasi Kalau Sesi Berikutnya Melanjutkan
1. Kalau user memutuskan target hosting/provider SMS-push/kebijakan retensi data — kerjakan sesuai keputusan itu, jangan diasumsikan sendiri (lihat Backlog di `TODO.md`)
2. Perluas seed data ke skala FRD (50-100 anggota) begitu ada modul yang benar-benar butuh melihat skala itu (Analitik & Laporan sudah lumayan butuh — 8 provinsi masih kelihatan tipis)
3. Formula final `calculateReadinessScore()` & bobot AI Mobilization masih placeholder (nilai seed semi-acak, bobot sudah bisa dikonfigurasi tapi filosofi perhitungannya sendiri belum divalidasi user) — jangan finalisasi formula produksi tanpa konfirmasi user
4. **Catatan teknis**: kalau menambah field foreign-key baru ke model yang sudah dipakai luas (kayak `Notifikasi.misi` di Fase 11), cek dulu apakah relasi Prisma-nya benar-benar didefinisikan bidirectional (field array di kedua sisi) — field `xxxId` polos tanpa `@relation` tidak bisa di-`include`, gejalanya baru ketahuan saat runtime query, bukan saat `tsc`
5. **Catatan teknis**: kalau mengubah `src/lib/prisma.ts` (nambah middleware/extension apa pun), langsung jalankan smoke-check ke semua route Command Center + Sisi Anggota setelahnya — perubahan sekecil apa pun di file itu bisa mengubah hasil tree-shaking bundler dan membocorkan Prisma ke client bundle tanpa ada error di `tsc`/`build` (cuma kelihatan sebagai runtime error di console browser). Aturan lengkapnya sekarang ada di `app/README.md`.
